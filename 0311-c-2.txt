// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int num; // num 숫자
    printf("숫자를 입력하세요:");
    scanf("%d", &num);
    //조건문
    if(num % 2 == 0) // if조건문 ( ) 나머지 if조건문은 세미콜론 안 붙임
    {printf("짝수입니다\n");
    printf("2로 나눈 나머지가 0\n");} //조건문이 2개 이상이라면 중괄호"{ }"를 붙여야 한다.
    else
    {printf("홀수입니다\n");
    printf("2로 나눈 나머지가 1\n");}
    return 0;
}



> 크다
<작다
=대입
== 같다
?=같지 않다
>= 크거나 같다